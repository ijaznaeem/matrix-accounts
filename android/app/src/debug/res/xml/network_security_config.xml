<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!--
        DEBUG-ONLY network security overrides.
        - Trusts user-installed CAs (e.g. Charles Proxy, mitmproxy).
        - Allows cleartext (HTTP) traffic to localhost / Android emulator host
          so the Flutter tool and local test servers work without TLS.
        - All other traffic still uses the production policy (system CAs, HTTPS).
    -->

    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>

    <!-- Emulator host (10.0.2.2) and localhost – allow HTTP for dev servers -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>

</network-security-config>
